<Page x:Class="WpfApp001.View.Mail_Frame"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp001.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Mail_Frame">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Pasek boczny - Lista osób -->
        <Border Style="{StaticResource CustomBorderStyle}" BorderBrush="{StaticResource SecondaryLightBrush}" BorderThickness="2" Margin="5" CornerRadius="10">
            <StackPanel Grid.Column="0" Margin="5">
                <TextBlock Style="{StaticResource GlobalTextBlockStyle}" Text="Kontakty" Margin="10" FontSize="16" FontWeight="Bold" />
                <ItemsControl ItemsSource="{Binding Contacts}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Name}" 
                                    Command="{Binding DataContext.SelectContactCommand, RelativeSource={RelativeSource AncestorType=Page}}" 
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" 
                                    Margin="5" FontWeight="Bold" Foreground="#FFD0D6D6" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- Lista wiadomości -->
        <ListBox Grid.Column="1" Margin="10,10,10,72" Style="{StaticResource CustomListBoxStyle}" ItemsSource="{Binding Messages}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Sender}" FontWeight="Bold" />
                        <TextBlock Text="{Binding Content}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Pole do pisania wiadomości -->
        <StackPanel Grid.Column="1" VerticalAlignment="Bottom" Orientation="Horizontal" Margin="5">
            <TextBox Width="600" Height="40" Margin="5" Style="{StaticResource CustomTextBoxStyle}" Text="{Binding NewMessage, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Wyślij" Width="80" Margin="5" VerticalAlignment="Center" Style="{StaticResource CustomSendButtonStyle}" Command="{Binding SendMessageCommand}" />
        </StackPanel>
    </Grid>
</Page>