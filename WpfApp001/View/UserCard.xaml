<Page x:Class="WpfApp001.View.UserCard"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp001.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="UserCard">

    <Grid>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

                <TextBlock Text="Użytkownik:" FontWeight="Bold" FontSize="16"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Margin="10,0,0,0" FontSize="16"><Run Text="[UserNa"/><Run Text="me]"/></TextBlock>

            <TextBlock Text="Stanowisko:" FontWeight="Bold" FontSize="16" Grid.Row="1"/>
                <TextBlock Text="[Position]" Grid.Row="1" Grid.Column="1" Margin="10,0,0,0" FontSize="16"/>

                <TextBlock Text="Data i godzina:" FontWeight="Bold" FontSize="16" Grid.Row="2"/>
            <TextBlock Text="{Binding CurrentDateTime}" Grid.Row="2" Grid.Column="1" Margin="10,0,0,0" FontSize="16"/>
            </Grid>
    </Grid>
</Page>

