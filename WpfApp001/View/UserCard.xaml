<Page x:Class="WpfApp001.View.UserCard"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp001.View"
      mc:Ignorable="d"
      Title="UserCard" Height="86" Width="280">

    <Grid>
        <!-- Wrap the TextBlocks in a Border with rounded edges -->
        <Border Background="#86b9b0" CornerRadius="10" Padding="10" MinWidth="280">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- User Name -->
                <TextBlock Text="Użytkownik:" FontWeight="Bold" FontSize="16"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Margin="10,0,-72,0" FontSize="16">
                    <Run Text="[UserNa"/><Run Text="me]"/>
                </TextBlock>

                <!-- Position -->
                <TextBlock Text="Stanowisko:" FontWeight="Bold" FontSize="16" Grid.Row="1"/>
                <TextBlock Text="[Position]" Grid.Column="1" Margin="10,21,-27,21" FontSize="16" Grid.RowSpan="3"/>

                <!-- Date and Time -->
                <TextBlock Text="Data i godzina:" FontWeight="Bold" FontSize="16" Grid.Row="2"/>
                <TextBlock Text="{Binding CurrentDateTime}" Grid.Row="2" Margin="10,0,-62,0" FontSize="16" Grid.Column="1"/>
            </Grid>
        </Border>
    </Grid>
</Page>

